// hooks.js
// Ganchos para lógica adicional generada por IA.

export function onLevelLoaded(/* state */) {
  // Hook al cargar nivel
}

export function onLevelChanged(/* state */) {
  // Hook al modificar nivel
}

export function onBeforeSave(/* state */) {
  // Hook antes de guardar
}

export function onAfterSave(/* state */) {
  // Hook después de guardar
}
